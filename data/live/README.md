# Live data inputs

This directory is reserved for non-secret, reproducible Live inputs used by the scheduled dashboard workflow.

Do not commit credentials or private provider exports here. The scheduled workflow expects `data/live/spm.db` and `data/live/oos.csv` when running from the repository checkout.
